---
description: BLoC conventions — naming, sealed classes, part files, state handling, Equatable, and DI registration
globs: lib/features/**/bloc/*.dart
alwaysApply: false
---

# BLoC Conventions

## 3-File Rule with `part`/`part of`

Every BLoC lives in `presentation/bloc/` as exactly 3 files. Events and states use
Dart's `part`/`part of` so they form one logical unit under the BLoC file:

```
auth_bloc.dart    ← declares the parts
auth_event.dart   ← part of auth_bloc.dart
auth_state.dart   ← part of auth_bloc.dart
```

```dart
// auth_bloc.dart
part 'auth_event.dart';
part 'auth_state.dart';

class AuthBloc extends Bloc<AuthEvent, AuthState> { ... }
```

```dart
// auth_event.dart
part of 'auth_bloc.dart';

sealed class AuthEvent extends Equatable {}
// event classes here...
```

```dart
// auth_state.dart
part of 'auth_bloc.dart';

sealed class AuthState extends Equatable {}
// state classes here...
```

> ⚠️ Part files cannot be imported directly. Always import `auth_bloc.dart` — it
> brings in events and states automatically.

## Equatable — Required on All Events & States

All event and state base classes **must** extend `Equatable`. Every subclass **must**
override `props` to list the fields that determine equality.

```dart
// pubspec.yaml — ensure this dependency exists
// equatable: ^2.0.5
```

**Why Equatable?**

BLoC uses `==` equality to decide whether to emit a new state. Without Equatable,
Dart compares objects by reference (identity), so two identical states are never
equal — causing unnecessary UI rebuilds. Equatable overrides `==` and `hashCode`
based on `props`, enabling BLoC to skip emitting when the state hasn't changed.

| Without Equatable | With Equatable |
|---|---|
| `AuthError("oops") == AuthError("oops")` → `false` (different instances) | `AuthError("oops") == AuthError("oops")` → `true` |
| Every `emit()` triggers a rebuild, even duplicates | BLoC skips rebuild when new state equals current state |
| Harder to test state equality with `expect()` | `blocTest` assertions work with simple value comparison |

## Events — Sealed + Prefixed + Equatable

Events are named `<Feature><Action><Verb>`:

```dart
// ✅ CORRECT
sealed class AuthEvent extends Equatable {
  @override
  List<Object?> get props => [];
}

final class AuthLoginRequested extends AuthEvent {
  final String email, password;
  const AuthLoginRequested({required this.email, required this.password});

  @override
  List<Object?> get props => [email, password];
}

final class AuthLogoutRequested extends AuthEvent {}
final class AuthStatusChecked   extends AuthEvent {}

// ❌ WRONG — no prefix, not sealed, no Equatable
abstract class AuthEvent {}
class LoginRequested extends AuthEvent {}
```

> Events with no fields can leave `props` empty (inherited from base) — still extend
> `Equatable` for consistency and future-proofing.

## States — Sealed + Equatable

```dart
sealed class AuthState extends Equatable {
  @override
  List<Object?> get props => [];
}

class AuthInitial extends AuthState {}

class AuthLoading extends AuthState {}

class AuthAuthenticated extends AuthState {
  final User user;
  const AuthAuthenticated(this.user);

  @override
  List<Object?> get props => [user];
}

class AuthUnauthenticated extends AuthState {}

class AuthError extends AuthState {
  final String message;
  const AuthError(this.message);

  @override
  List<Object?> get props => [message];
}
```

> `AuthLoading` and `AuthInitial` have no fields — inheriting `props: []` from
> the base is sufficient. BLoC will still deduplicate consecutive identical emissions.

## BLoC Handler Pattern

Event handlers are **always extracted into separate private methods**. Never write logic inline inside `on<>()`.

Naming convention: `_on<EventName>` — exactly mirrors the event class name with a `_on` prefix.

```dart
// ✅ CORRECT — register with method reference
class AuthBloc extends Bloc<AuthEvent, AuthState> {
  AuthBloc({required this.loginUseCase, required this.logoutUseCase})
      : super(AuthInitial()) {
    on<AuthLoginRequested>(_onAuthLoginRequested);
    on<AuthLogoutRequested>(_onAuthLogoutRequested);
    on<AuthStatusChecked>(_onAuthStatusChecked);
  }

  Future<void> _onAuthLoginRequested(
    AuthLoginRequested event,
    Emitter<AuthState> emit,
  ) async {
    emit(AuthLoading());
    final response = await loginUseCase(
      LoginParams(email: event.email, password: event.password),
    );
    switch (response) {
      case ApiSuccess(:final data): emit(AuthAuthenticated(data));
      case ApiError(:final message): emit(AuthError(message));
    }
  }

  Future<void> _onAuthLogoutRequested(
    AuthLogoutRequested event,
    Emitter<AuthState> emit,
  ) async { ... }

  Future<void> _onAuthStatusChecked(
    AuthStatusChecked event,
    Emitter<AuthState> emit,
  ) async { ... }
}

// ❌ WRONG — inline logic directly in on<>()
on<AuthLoginRequested>((event, emit) async {
  emit(AuthLoading());
  // ...
});
```

## UI — Use switch for exhaustive state handling

```dart
// ✅ switch forces all states to be handled
switch (state) {
  case AuthInitial():              return const SplashView();
  case AuthLoading():              return const LoadingSpinner();
  case AuthAuthenticated(:final user): return HomeView(user: user);
  case AuthUnauthenticated():      return const LoginPage();
  case AuthError(:final message):  return ErrorView(message: message);
}
```

## DI Registration

```dart
// ✅ BLoC = registerFactory (fresh instance per navigation)
sl.registerFactory(() => AuthBloc(loginUseCase: sl(), logoutUseCase: sl()));

// ❌ NEVER registerSingleton or registerLazySingleton for BLoC
```
